void main(){

  // common data types
  
  // plain text (String)
  String firstName = "Nyel";

  // number (int)
  int age = 22;

  // decimals (double)
  double price = 24.55;

  // boolean can ba true or false(bool)
  bool isConnected = true;

  // all values can be printed out
  print(firstName);
  print(age);
  print(price);
  print(isConnected);

}